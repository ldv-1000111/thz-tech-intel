version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  commands:
    - pip install -r docs/requirements.txt
    - sphinx-build -b html docs $READTHEDOCS_OUTPUT/html
    - sphinx-build -b revealjs docs $READTHEDOCS_OUTPUT/html/slides

sphinx:
  configuration: docs/conf.py
